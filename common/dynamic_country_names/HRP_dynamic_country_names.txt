DEN = { # Sumatra
	dynamic_country_name = {
		name = dyn_c_denmark_norway
		adjective = DEN_ADJ

		is_main_tag_only = yes
		priority = 10

		trigger = {
			exists = scope:actor
			scope:actor ?= {
				OR = {
					owns_entire_state_region = STATE_EASTERN_NORWAY
					owns_entire_state_region = STATE_NORTHERN_NORWAY
					owns_entire_state_region = STATE_EASTERN_NORWAY
				}
			}
		}
	}
}

GBR = { 
	dynamic_country_name = {
		name = GBR_chartists
		adjective = GBR_ADJ

		is_main_tag_only = yes
		priority = 10

		trigger = {
			exists = scope:actor
			scope:actor ?= {
				owns_entire_state_region = STATE_WALES
				NOT = {
					owns_entire_state_region = STATE_HOME_COUNTIES
				}
			}
		}
	}
}

PRC = { 
	dynamic_country_name = {
		name = generic_revolt_legitimist
		adjective = generic_revolt_legitimist_adj

		priority = 1000

		trigger = {
			exists = scope:actor
			scope:actor ?= {
				country_has_monarchy_law = yes
			}
		}
	}
}

HAM = { 
	dynamic_country_name = {
		name = zillian_republic
		adjective = zillian_republic_adj

		priority = 1000

		trigger = {
			exists = scope:actor
			scope:actor ?= {
				has_law_or_variant = law_type:law_parliamentary_republic
				has_law_or_variant = law_type:law_universal_suffrage
			}
		}
	}
}

BAV = { 
	dynamic_country_name = {
		name = BAV_WUR
		adjective = BAV_ADJ

		is_main_tag_only = yes
		priority = 10

		trigger = {
			exists = scope:actor
			scope:actor ?= {
					owns_entire_state_region = STATE_WURTTEMBERG
			}
		}
	}
}
TEX = {
	dynamic_country_name = {
		name = dyn_c_texan_columbia
		adjective = dyn_c_texan_columbia_adj

		priority = 1000

		trigger = { #owns a part of historic Gran Colombia
			exists = scope:actor
			scope:actor ?= {
				OR = {
					owns_entire_state_region = STATE_ECUADOR
					owns_entire_state_region = STATE_ZULIA
					owns_entire_state_region = STATE_MIRANDA
					owns_entire_state_region = STATE_BOLIVAR
					owns_entire_state_region = STATE_ANTIOQUIA
					owns_entire_state_region = STATE_GUAVIARE
					owns_entire_state_region = STATE_CUNDINAMARCA
					owns_entire_state_region = STATE_CAUCA
					owns_entire_state_region = STATE_PASTAZA
					owns_entire_state_region = STATE_PANAMA
				}
			}
		}
	}
}