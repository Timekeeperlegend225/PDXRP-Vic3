GM_kill_char = {
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/exile_character"

	potential = {
		owner ?= {
			has_modifier = game_master
		}
	}

	show_requirements = yes

	possible = {
		always = yes
	}

	effect = {
		ordered_country = {
			limit = {
				has_modifier = GM_target
			}
			save_scope_as = owner_scope 
		}
		transfer_character = scope:owner_scope
		kill_character = yes
	}
}

GM_grant_leadership = {
	icon = "gfx/interface/character_panel/grant_command_not.dds"
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/agitator_promote"

	potential = {
		owner ?= {
			has_modifier = game_master
		}
	}

	possible = {
		always = yes
	}

	effect = {
		ordered_country = {
			limit = {
				has_modifier = GM_target
			}
			save_scope_as = owner_scope 
		}
		transfer_character = scope:owner_scope
		remove_character_role = agitator
		add_character_role = politician
		set_as_interest_group_leader = yes
	}
}

GM_grant_rulership = {
	icon = "gfx/interface/character_panel/grant_command_not.dds"
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/agitator_promote"

	potential = {
		owner ?= {
			has_modifier = game_master
		}
	}

	possible = {
		always = yes
	}

	effect = {
		ordered_country = {
			limit = {
				has_modifier = GM_target
			}
			save_scope_as = owner_scope 
		}
		transfer_character = scope:owner_scope
		remove_character_role = agitator
		add_character_role = politician
		set_character_as_ruler = yes
	}
}


GM_set_as_heir = {
	icon = "gfx/interface/character_panel/grant_command_not.dds"
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/agitator_promote"

	potential = {
		owner ?= {
			has_modifier = game_master
		}
	}

	possible = {
		always = yes
	}

	effect = {
		ordered_country = {
			limit = {
				has_modifier = GM_target
			}
			save_scope_as = owner_scope
			set_heir = root
		}
		transfer_character = scope:owner_scope
		remove_character_role = agitator
		add_character_role = politician
	}
}


GM_send_back_as_agitator = {
	icon = "gfx/interface/character_panel/grant_command_not.dds"
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/agitator_promote"

	potential = {
		owner ?= {
			has_modifier = game_master
		}
	}

	possible = {
		always = yes
	}

	effect = {
		ordered_country = {
			limit = {
				has_modifier = GM_target
			}
			save_scope_as = owner_scope 
		}
		transfer_character = scope:owner_scope
	}
}

transfer_to_GM = {
	icon = "gfx/interface/character_panel/grant_command_not.dds"
	clicksound = "event:/SFX/DLC/1.3_ip1/UI/agitator_promote"

	potential = {
		owner ?= {
			has_modifier = GM_target
		}
	}

	possible = {
		always = yes
	}

	effect = {
		add_character_role = agitator
		ordered_country = {
			limit = {
				has_modifier = game_master
			}
			save_scope_as = GM_scope
		}
		transfer_character = scope:GM_scope
}
