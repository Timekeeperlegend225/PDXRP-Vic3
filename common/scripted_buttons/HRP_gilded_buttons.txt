GA_buy_action = {
	name = "Buy stock"
	desc = "Buy a stock as the sate on the global stock exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		custom_tooltip = {
			text = "Not enough money"
			var:GM_action_cost < var:temp_money_available
		}
		custom_tooltip = {
			text = "You can only buy an action every months"
			NOT = { has_variable = buy_action_cooldown }
		}
	}

	effect = {
		add_treasury = var:GM_action_cost_neg
		custom_tooltip = {
			text = "You get an action"
			change_variable = {
				name = Gilded_actions
				add = 1
			}
		}
		set_variable = { 
			name = buy_action_cooldown 
			months = 1
		}
		je:je_HRP_gilded_age_gm = {
			add_progress = { 
				name = HRP_GA_infinite_growth 
				value = global_var:gm_variable
			}
		}
		set_global_variable = { 
			name = sell_action_global_temp 
			value = var:GM_action_cost_neg
		}
		change_global_variable = { 
			name = sell_action_global_temp 
			divide = -500000
		}
		custom_tooltip = {
			text = "This will decrease the global stock exchange value a bit"
			every_country = {
				je:je_HRP_gilded_age_gm = {
					add_progress = { 
						name = HRP_GA_infinite_growth 
						value = global_var:sell_action_global_temp
					}
				}
				je:je_HRP_gilded_age = {
					add_progress = { 
						name = HRP_GA_infinite_growth 
						value = global_var:sell_action_global_temp
					}
				}
			}
		}
	}
}

GA_sell_action = {
	name = "Sell stock"
	desc = "Sell one of your stocks as the sate on the global stock exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		custom_tooltip = {
			text = "No action to sell"
			var:Gilded_actions > 0
		}
		custom_tooltip = {
			text = "You can only sell an action every months"
			NOT = { has_variable = sell_action_cooldown }
		}
	}

	effect = {
		add_treasury = var:GM_action_cost
		custom_tooltip = {
			text = "You sell an action"
			change_variable = {
				name = Gilded_actions
				subtract = 1
			}
		}
		set_variable = { 
			name = sell_action_cooldown 
			months = 1
		}
		set_global_variable = { 
			name = sell_action_global_temp 
			value = var:GM_action_cost_neg
		}
		change_global_variable = { 
			name = sell_action_global_temp 
			divide = 500000
		}
		custom_tooltip = {
			text = "This will decrease the global stock exchange value a bit"
			every_country = {
				je:je_HRP_gilded_age_gm = {
					add_progress = { 
						name = HRP_GA_infinite_growth 
						value = global_var:sell_action_global_temp
					}
				}
				je:je_HRP_gilded_age = {
					add_progress = { 
						name = HRP_GA_infinite_growth 
						value = global_var:sell_action_global_temp
					}
				}
			}
		}
	}
}

GA_encourage_speculation = {
	name = "Encourage Speculation"
	desc = "Encourage your captialists to take part in the Global Stock Exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		NOT = {
			OR = {
				has_modifier = HRP_encouraged_speculation
				has_modifier = HRP_discouraged_speculation
				has_law = law_type:law_isolationism
				has_law = law_type:law_command_economy
				has_law = law_type:law_industry_banned
				has_law = law_type:law_extraction_economy
			}
		}
		custom_tooltip = {
			text = "Don't change your mind so soon"
			NOT = { has_variable = GA_speculation_cooldown }
		}
	}

	effect = {
		custom_tooltip = {
			text = "The modifier increase your integration in the global stock exchange"
		}
		add_modifier = HRP_encouraged_speculation
		set_variable = { 
			name = GA_speculation_cooldown
			months = 6
		}
	}
}

GA_stop_encouraging_speculation = {
	name = "Stop Encouraging Speculation"
	desc = "Stop Encouraging your captialists to take part in the Global Stock Exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		has_modifier = HRP_encouraged_speculation
		custom_tooltip = {
			text = "Don't change your mind so soon"
			NOT = { has_variable = GA_speculation_cooldown }
		}
	}

	effect = {
		remove_modifier = HRP_encouraged_speculation
		set_variable = { 
			name = GA_speculation_cooldown 
			months = 6
		}
	}
}

GA_discourage_speculation = {
	name = "Discourage Speculation"
	desc = "Discourage your captialists to take part in the Global Stock Exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		NOT = {
			OR = {
				has_modifier = HRP_encouraged_speculation
				has_modifier = HRP_discouraged_speculation
				has_law = law_type:law_laissez_faire
				has_law = law_type:law_free_trade
			}
		}
		custom_tooltip = {
			text = "Don't change your mind so soon"
			NOT = { has_variable = GA_speculation_cooldown }
		}
	}

	effect = {
		custom_tooltip = {
			text = "The modifier decrease your integration in the global stock exchange"
		}
		add_modifier = HRP_discouraged_speculation
		set_variable = { 
			name = GA_speculation_cooldown
			months = 6
		}
	}
}

GA_stop_discouraging_speculation = {
	name = "Stop Discouraging Speculation"
	desc = "Stop Discouraging your captialists to take part in the Global Stock Exchange"
	
	visible = { 
		always = yes
	}

	possible = {
		has_modifier = HRP_discouraged_speculation
		custom_tooltip = {
			text = "Don't change your mind so soon"
			NOT = { has_variable = GA_speculation_cooldown }
		}
	}

	effect = {
		remove_modifier = HRP_discouraged_speculation
		set_variable = { 
			name = GA_speculation_cooldown 
			months = 6
		}
	}
}