fm_current_debt = {
	value = credit
	multiply = scaled_debt
}
fm_remaining_credit = {
	value = credit
	subtract = fm_current_debt
}
fm_transferable_currency = {
	value = gold_reserves
	add = fm_remaining_credit
}